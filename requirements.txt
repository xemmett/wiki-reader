# Device (Pi + laptop testing). Pillow is the only hard dependency.
Pillow>=10.0.0

# On the Pi, also install the button + panel backends (setup.sh does this):
# GPIO buttons + SPI for the panel
spidev 
rpi-lgpio 
lgpio gpiozero   
# Piwi Connect web portal
fastapi 
uvicorn 
python-multipart
# The panel driver (waveshare_epd) is NOT on PyPI — vendor the two .py files
# from Waveshare's repo into device/waveshare_epd/ (see README).
#
# The FastAPI backend + Wikipedia/EPUB/PDF importers (Phase 3+) are a SEPARATE
# service, not installed on the device. libzim etc. live there, not here.
